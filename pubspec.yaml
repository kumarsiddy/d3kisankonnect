---
name: d3kisankonnect
description: A new Flutter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^0.1.3
  dartz: ^0.9.1
  freezed_annotation: ^0.11.0
  flutter_bloc: ^6.0.1
  injectable: 1.0.2
  get_it: ^4.0.4
  flushbar: ^1.10.4
  flutter_form_builder: 3.10.1 #(Need to research)
  rxdart: 0.24.1
  retrofit: ^1.3.4
  dio: ^3.0.9
  json_annotation: ^3.0.1
  hive: ^1.4.2
  hive_flutter: ^0.3.1
  path_provider: ^1.6.11
  provider: ^4.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: null
  freezed: ^0.11.4
  injectable_generator: ^1.0.3
  lint: ^1.2.0
  retrofit_generator: ^1.3.7+5
  json_serializable: ^3.3.0
  hive_generator: ^0.7.1

flutter:
  uses-material-design: true

  assets:
    - config/
    - lang/en.json
    - lang/hi.json
